#include "../kernel/user/user.h"

int main() {
    //printf("this is console\n");

    while (1);
    
    while (1)
    {
        int count = 0;
        while (count++ < 10000);
        printf("this is console\n");
    }
    
    // int pid;
    // pid = fork();
    while (1);
    return 0;
}